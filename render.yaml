services:
  - type: web
    name: rag-chatbot-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn rag_app:app --host 0.0.0.0 --port 10000
    plan: starter
    envVars:
      - key: PYTHON_VERSION
        value: 3.10